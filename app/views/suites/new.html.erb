<div class="container m-t-1 m-b-1">
  <%= link_to product_version_suites_path(@product,@version), class: 'btn btn-primary pull-xs-right m-x-auto' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <h1>New suite for version <%= @version.name %></h1>
</div>

<%= render 'form' %>
