<div class="container m-t-1 m-b-1">
  <%= link_to product_version_test_cases_path(@product,@version), class: 'btn btn-outline-primary pull-md-right m-l-1' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to product_version_test_case_path(@product,@version,@test_case), class: 'btn btn-primary pull-md-right  m-l-1' do %>
    <span class="glyphicon glyphicon-info-sign"></span>
    Show
  <% end %>
  <h1>Editing test_case</h1>
</div>

<%= render 'form' %>
